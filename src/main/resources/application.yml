spring:
  datasource:
    url: jdbc:mysql://localhost:3306/heydaze?useSSL=false&serverTimezone=Asia/Seoul&useUnicode=true&characterEncoding=UTF8
    username: root
    password: root0327
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    open-in-view: false

logging:
  level:
    root: info
    page_110.app.messages.AuditingFilter: debug
